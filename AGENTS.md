# 项目协作约定

- 每次完成代码修改并验证后，先询问用户是否将本次修改提交并推送到 GitHub。只有用户明确同意后，才执行 `git commit` 和 `git push`；一次同意只适用于当前批次修改。
- 用户明确要求提交当前改动时，可以直接完成该批次提交和推送，不重复确认。
- 提交应限于本次授权范围。保留已有用户修改，不擅自重置历史或强制推送。
- 数据集、模型权重、本机配置、凭据及下载缓存不纳入普通 Git 提交。绝不在日志、文档或提交中暴露密码、密钥、令牌。
- 改动保持最小范围；提交前运行适当检查，并明确说明没有实际验证的部分。
